[project]
name = "web_api"
version = "0.1.0"
description = "Generic API to simulate network calls for energy efficiency research"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.1",
    "httpx>=0.28.1",
    "pytest>=8.4.1",
    "pytest-asyncio>=1.1.0",
    "uvicorn>=0.35.0",
]
